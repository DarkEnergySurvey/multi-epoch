%define name @PACKAGE_NAME@
%define version @PACKAGE_VERSION@
%define release 1mp

Summary: Simulate astronomical images
Name: %{name}
Version: %{version}
Release: %{release}
Source0: ftp://ftp.iap.fr/pub/from_users/bertin/skymaker/%{name}-%{version}.tar.gz
URL: http://terapix.iap.fr/soft/skymaker/
License: LGPL
Group: Sciences/Astronomy
BuildRoot: %{_tmppath}/%{name}-buildroot
Prefix: %{_prefix}

%description
SkyMaker is a program that simulates astronomical images in FITS format.

%prep
%setup -q

%build
if test "$TPXFLAGS"; then
./configure --enable-threads --enable-static --enable-icc --prefix=$RPM_BUILD_ROOT/usr/local/ --mandir=$RPM_BUILD_ROOT/usr/local/man/
make -j CC="$TPXCC" CFLAGS="$TPXFLAGS" LIBS="$TPXLIBS"
else
./configure  --enable-threads --prefix=$RPM_BUILD_ROOT/usr/local/ --mandir=$RPM_BUILD_ROOT/usr/local/man/
make -j
fi

%install
make install

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root)
/usr/local/bin/sky
/usr/local/man/man1/sky.1
/usr/local/man/manx/sky.x
%doc AUTHORS ChangeLog COPYING HISTORY INSTALL README THANKS

%changelog
* @DATE2@ @PACKAGER@ <@PACKAGE_BUGREPORT@>
- Automatic RPM rebuild
* Wed Mar 20 2003 Emmanuel Bertin <bertin@iap.fr>
- First RPM build for V3.0

# end of file
